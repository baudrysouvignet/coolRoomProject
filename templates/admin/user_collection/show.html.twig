{% extends 'base.html.twig' %}

{% block title %}UserCollection{% endblock %}

{% block body %}
    <h1>UserCollection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_collection.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user_collection.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_collection_index') }}">back to list</a>

    <a href="{{ path('app_user_collection_edit', {'id': user_collection.id}) }}">edit</a>

{% endblock %}
